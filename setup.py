from setuptools import setup
setup(name='dgut-inet-auth',
      version='0.1',
      author='Zhu Chuang',
      author_email='chuang at melty dot land',
      packages=[],
      scripts=['dgut-inet-auth'],
      python_requires='>=3.7',
      install_requires=['requests', 'pycryptodome'])
